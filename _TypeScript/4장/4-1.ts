// enum 타입으로 요일 정리

enum Weekdays {
    Monday = 1,
    Tuesday = 2,
    Wednesday = 3,
    Thursday = 4,
    Friday = 5,
    Saturday = 6,
    Sunday = 7
}

let dayOff = Weekdays.Friday;
console.log(Weekdays[3]);
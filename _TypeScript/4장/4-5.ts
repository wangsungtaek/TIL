// 문자열 열거

enum Direction {
    Up = 'UP',
    Down = 'DOWN',
    Left = 'LEFT',
    Right = 'RIGHT'
}

function move(where: Direction){
    if( where === Direction.Up ) {

    }
}

move(Direction.Down);